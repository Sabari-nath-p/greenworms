import 'package:flutter/material.dart';

class JSProgressCard extends StatelessWidget {
  const JSProgressCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
